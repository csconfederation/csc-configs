// =========================================================
// CSC Config File
// Path: Match/cfg/MatchZy/warmup.cfg
// Version: f5a42a2
// Last Updated: 2025-10-23
// =========================================================

// =========================================================
// Server / Voice / Debug
// =========================================================
sv_cheats 0                           // Cheats disabled. Default: 0
sv_deadtalk 1                         // Dead can talk to team. Default: 1
sv_full_alltalk 0                     // No always-on full alltalk. Default: 0
sv_infinite_ammo 0                    // No infinite ammo. Default: 0
sv_showimpacts 0                      // Disable bullet impact debug. Default: 0
sv_voiceenable 1                      // Voice comms enabled. Default: 1


// =========================================================
// Bots
// =========================================================
bot_kick                              // Remove all bots from the server. Default: (none)
bot_quota 0                           // Do not add bots automatically. Default: 10


// =========================================================
// Default Player Loadouts
// =========================================================
mp_ct_default_melee "weapon_knife"    // CT default melee. Default: weapon_knife
mp_ct_default_primary "weapon_m4a1"   // CT default primary (warmup testing). Default: ""
mp_ct_default_secondary "weapon_deagle" // CT default pistol (warmup). Default: weapon_hkp2000
mp_t_default_melee "weapon_knife"     // T default melee. Default: weapon_knife
mp_t_default_primary "weapon_ak47"    // T default primary (warmup testing). Default: ""
mp_t_default_secondary "weapon_deagle" // T default pistol (warmup). Default: weapon_glock


// =========================================================
// Economy
// =========================================================
cash_team_bonus_shorthanded 0         // Disable shorthanded cash bonus. Default: 1
mp_maxmoney 16000                     // Max player money. Default: 16000
mp_startmoney 16000                   // Start money (warmup). Default: 800


// =========================================================
// Gameplay Rules
// =========================================================
mp_autokick 0                         // Don’t kick idle players during warmup. Default: 1
mp_autoteambalance 0                  // Don’t auto-balance teams. Default: 1
mp_buy_anywhere 0                     // Disallow buying anywhere. Default: 0
mp_buytime 15                         // Buy time in seconds. Default: 15
mp_death_drop_gun 0                   // Do not drop guns on death (keeps flow). Default: 1
mp_free_armor 0                       // No free armor by default. Default: 0
mp_ignore_round_win_conditions 0      // Normal win conditions. Default: 0
mp_limitteams 0                       // No team size limit (warmup). Default: 2
mp_maxrounds 24                       // Max rounds if warmup ends into scrims. Default: 30
mp_randomspawn "1"                    // Enable random spawns (warmup). Default: 0
mp_randomspawn_los "1"                // Random spawn requires line of sight. Default: 0
mp_respawn_on_death_ct 0              // No CT respawn on death. Default: 0
mp_respawn_on_death_t 0               // No T respawn on death. Default: 0
mp_solid_teammates 0                  // Teammates are non-solid. Default: 1
mp_spectators_max 20                  // Allow up to 20 spectators. Default: 0
mp_teammates_are_enemies "0"          // Friendly fire/TSK disabled. Default: 0
mp_timelimit 0                        // No map time limit (warmup). Default: 0
mp_weapons_allow_typecount -1         // Allow all weapon types (no limit). Default: -1
mp_warmup_pausetimer 1                // Pause warmup timer (runs until live). Default: 0
mp_warmuptime 9999                    // Long warmup duration (seconds). Default: 60
mp_warmup_start                       // Start warmup now. Default: (command)


// =========================================================
// End of Config
// =========================================================
say "> CSC Config Loaded | warmup.cfg | <commit hash> | <date> <"
