// =========================================================
// CSC Config File
// Path: Combine/cfg/gamemode_competitive_server.cfg
// Version: 2ecfeae
// Last Updated: 2025-10-23
// =========================================================



// Bot Settings
bot_quota                        "0"     // Number of bots to add. Default: 10
bot_quota_mode                   fill    // Fill empty player slots with bots. Default: normal


// Default Player Loadouts
mp_ct_default_melee              weapon_knife   // Default CT melee weapon. Default: weapon_knife
mp_ct_default_primary            ""             // Default CT primary weapon (none). Default: ""
mp_ct_default_secondary          weapon_hkp2000 // Default CT pistol. Default: weapon_hkp2000
mp_t_default_melee               weapon_knife   // Default T melee weapon. Default: weapon_knife
mp_t_default_primary             ""             // Default T primary weapon (none). Default: ""
mp_t_default_secondary           weapon_glock   // Default T pistol. Default: weapon_glock


// Gameplay Rules
mp_death_drop_gun                "1"     // Players drop their weapon on death. Default: 1
mp_teammates_are_enemies         "0"     // Teammates can damage each other? 0 = no. Default: 0


// Match Flow & Timing
mp_freezetime                    "20"    // Time before each round starts (seconds). Default: 15
mp_min_halftime_duration         "30"    // Minimum halftime duration (seconds). Default: 15
mp_overtime_limit                "1"     // Limit overtime to a single set for scrims. Default: 0
mp_overtime_startmoney           "12500" // Start money during overtime halves. Default: 10000
mp_round_restart_delay           "7"     // Delay before next round starts (seconds). Default: 7


// Team Timeouts
mp_team_timeout_max              "4"     // Max timeouts per team per match. Default: 4
mp_team_timeout_ot_add_each      "0"     // Extra timeouts granted per OT half. Default: 0
mp_team_timeout_ot_add_once      "0"     // Grant OT timeouts only once? Default: 0
mp_team_timeout_ot_max           "4"     // Max OT timeouts per team. Default: 4
mp_team_timeout_time             "30"    // Timeout length in seconds. Default: 30


// Server Behavior
sv_allow_votes                   "0"     // Disable in-game votes. Default: 1
sv_auto_cstrafe_kick             true    // Kick players using movement macros. Default: true
sv_auto_cstrafe_logging          "1"     // Enable logging for autostrafe detection. Default: 0
sv_auto_full_alltalk_during_warmup_half_end "1"  // Enable all-talk during warmup and halftime for scrims. Default: 0
sv_deadtalk                      "0"     // Dead players can’t talk to living players. Default: 1
sv_deltaticks_enforce            0       // Disable delta-tick enforcement. Default: 0
sv_vote_issue_kick_allowed       "0"     // Disable player kick votes. Default: 1
tv_delay                         "0"     // GOTV broadcast delay (seconds). Scrim requirement: zero delay. Default: ~10–30


// End of Config
say "> CSC Config Loaded | gamemode_competitive_server.cfg | <commit hash> | <date> <"
